╔══════════════════════════════════════════════════════════════════════════════╗
║                          TSAGENT NPM SCRIPTS                                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

📦 BUILD & DEVELOPMENT
─────────────────────────────────────────────────────────────────────────────
  npm run build               Build desktop and CLI apps
  npm run build:desktop       Build desktop app only
  npm run build:cli           Build CLI app only
  npm run dev:desktop         Watch mode for desktop app development
  npm run dev:cli             Run CLI in development mode
  npm run start:desktop       Start desktop app
  npm run start:cli           Start CLI app

📤 PUBLISHING
─────────────────────────────────────────────────────────────────────────────
  npm run publish:all <ver>     Complete publishing workflow: convert to npm, bump version, publish
                                Example: npm run publish:all 1.4.0
  npm run publish:dry-run <ver>  Dry run of publish process
                                Example: npm run publish:dry-run 1.4.0

🔗 DEPENDENCY MANAGEMENT
─────────────────────────────────────────────────────────────────────────────
  npm run deps:dev            Complete dev workflow: clean, convert to file://, install
                               Use this when developing locally to link internal packages
  
  npm run deps:publish        Complete publish workflow: convert to npm, clean
                               Use this when preparing to publish packages
                               (does not install - publish:all handles installation)

  npm run deps:to-file        Convert internal dependencies to file:// format (granular)
  npm run deps:to-npm         Convert internal dependencies to npm versions (granular)
  npm run deps:install        Install dependencies in dependency order (granular)
  npm run deps:clean          Remove all node_modules and package-lock.json (granular)

📝 VERSION MANAGEMENT
─────────────────────────────────────────────────────────────────────────────
  npm run version:bump <ver>  Bump version across all packages (granular command)
                              Example: npm run version:bump 1.4.0
                              Updates all package.json versions and internal dependencies
                              
  Publishing workflow:
    npm run publish:all 1.4.0       # Complete workflow: convert to npm, bump version, publish

💡 TIP: Run 'npm run help' to see this documentation again
